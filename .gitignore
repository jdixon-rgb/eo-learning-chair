# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# Supabase CLI local cache (project-ref, CLI version pins, etc.)
supabase/.temp/

# Pre-push schema snapshots (created by docs/MIGRATION_PLAYBOOK.md before
# every prod migration push). Local-only; not tracked.
snapshots/
